name: Test Strategy - Afrasia
run-name: ${{ github.actor }} initiated a JMeter performance test
on:
  workflow_dispatch:
    inputs:
      loops:
        type: string
        description: "Loops"
      requests:
        type: string
        description: "Requests"
        default: "100"
jobs:
  test-flow:
    uses: ./.github/workflows/main.yml
    with:
      environment: "pp"
      jmx_file_name: "afrasia_strategy"
      strategy_id: "1486"
      loops: ${{ inputs.loops }}
      name_prefix: "lon-pp"
      requests: ${{ inputs.requests }}

